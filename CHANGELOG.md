# Changelog

## [0.1.0] - 2026-06-11

### Added

- Added `description`, `created`, and `updated` metadata fields to graph schema
- Updated `exgraph init` template to include new metadata fields
- Updated SKILL.md to document `exgraph init` command usage

### Fixed

- Fixed `viewer/main.ts` not included in npm package by updating `files` in package.json

### Changed

- Updated all example YAML files to include new metadata fields
